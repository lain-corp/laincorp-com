{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "laincorp-com",
  "scripts": {
    "build": "npm run build",
    "prebuild": "npm run",
    "pretest": "npm run",
    "start": "npm start",
    "test": "npm test"
  }
}